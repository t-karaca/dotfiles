[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true

[include]
    path = delta/current.gitconfig

[merge]
    tool = nvimdiff
    conflictstyle = diff3

[init]
    defaultBranch = main

[pull]
    rebase = false
